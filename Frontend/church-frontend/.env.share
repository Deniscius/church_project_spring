# Build local partagé (npm run prod:share) + vite preview / ngrok
# Même origine : /api → proxy Vite → backend :8081
# Tunnel : ngrok http 4173
VITE_API_BASE_URL=/api
VITE_PUBLIC_SITE_URL=http://localhost:4173
