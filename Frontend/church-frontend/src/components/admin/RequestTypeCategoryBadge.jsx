import React from 'react';
import AppCard from '../ui/AppCard';

export default function RequestTypeCategoryBadge() {
  return (
    <AppCard title="RequestTypeCategoryBadge" subtitle="Composant admin prêt à recevoir la logique métier.">
      <p style={{ margin: 0, color: 'var(--muted)' }}>Ce composant sert de point d'ancrage pour l'interface correspondante.</p>
    </AppCard>
  );
}
