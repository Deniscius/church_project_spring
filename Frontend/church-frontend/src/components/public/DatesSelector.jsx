import React from 'react';
import AppCard from '../ui/AppCard';

export default function DatesSelector() {
  return (
    <AppCard title="DatesSelector" subtitle="Composant de présentation prêt à brancher sur les données backend.">
      <p style={{ margin: 0, color: 'var(--muted)' }}>Zone réservée à dates selector.</p>
    </AppCard>
  );
}
