import React from 'react';
import AppCard from '../ui/AppCard';

export default function ParoisseSelector() {
  return (
    <AppCard title="ParoisseSelector" subtitle="Composant de présentation prêt à brancher sur les données backend.">
      <p style={{ margin: 0, color: 'var(--muted)' }}>Zone réservée à paroisse selector.</p>
    </AppCard>
  );
}
