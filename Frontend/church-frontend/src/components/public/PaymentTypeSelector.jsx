import React from 'react';
import AppCard from '../ui/AppCard';

export default function PaymentTypeSelector() {
  return (
    <AppCard title="PaymentTypeSelector" subtitle="Composant de présentation prêt à brancher sur les données backend.">
      <p style={{ margin: 0, color: 'var(--muted)' }}>Zone réservée à paymenttype selector.</p>
    </AppCard>
  );
}
