import React from 'react';
import AppCard from '../ui/AppCard';

export default function RequestTypeSelector() {
  return (
    <AppCard title="RequestTypeSelector" subtitle="Composant de présentation prêt à brancher sur les données backend.">
      <p style={{ margin: 0, color: 'var(--muted)' }}>Zone réservée à requesttype selector.</p>
    </AppCard>
  );
}
