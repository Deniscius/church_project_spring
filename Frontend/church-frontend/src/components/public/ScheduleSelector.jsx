import React from 'react';
import AppCard from '../ui/AppCard';

export default function ScheduleSelector() {
  return (
    <AppCard title="ScheduleSelector" subtitle="Composant de présentation prêt à brancher sur les données backend.">
      <p style={{ margin: 0, color: 'var(--muted)' }}>Zone réservée à schedule selector.</p>
    </AppCard>
  );
}
