import React from 'react';
import AppCard from '../ui/AppCard';

export default function LocalitiesTable() {
  return (
    <AppCard title="LocalitiesTable" subtitle="Composant super-admin prêt à être branché.">
      <p style={{ margin: 0, color: 'var(--muted)' }}>Ce bloc structure la zone d'administration globale.</p>
    </AppCard>
  );
}
